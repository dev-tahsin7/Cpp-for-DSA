#include <bits/stdc++.h>
using namespace std;

int main() {
    string name = "TaHsin"; 
    // sort(staring, ending+1)
    sort(name.begin(), name.end()); 
    cout << name << endl; 

    return 0;
}